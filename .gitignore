# deps / build
node_modules/
dist/
*.tsbuildinfo

# electron-builder packaging artifacts
app/staging/
app/release/

# env
.env
.env.local

# Navigraph navdata — licensed for personal use, do NOT commit.
# Anchored to repo root so it does NOT match the src/brain/navdata/ SOURCE folder.
/navdata/
*.s3db
*.db
*.sqlite

# local caches
cache/

# logs / os
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# stray electron stdout/stderr dumps
app/el.out.txt
app/el.err.txt

# local Claude Code config
.claude/

# training artifacts (regenerable; can be large)
training/data/
training/out/
.venv/
launch-dev.cmd
